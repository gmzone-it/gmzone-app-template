---
# DevSecOps pipeline for gmzone-app-template
#
# Stages (in order, all must pass for a release to happen):
#   1. test           → pytest on the FastAPI app
#   2. security-scan  → Gitleaks (secrets) + Semgrep (SAST) + OWASP Dep-Check (SCA)
#   3. build-publish  → Build → Trivy (image CVE) → SBOM → push GHCR → Cosign keyless sign
#
# Failure policy:
#   - HIGH or CRITICAL findings = pipeline FAILS, no image is published.
#   - MEDIUM/LOW = uploaded to GitHub Security tab as warnings, pipeline stays green.
#
# Triggers:
#   - push to main         → full pipeline, image tagged 'sha-<short>' + 'latest'
#   - push tag v*          → full pipeline, image tagged with the version
#   - pull_request         → test + security-scan only (no publish)
#
# Outputs:
#   - SARIF reports in the Security tab (Code scanning alerts)
#   - Signed multi-tag image on ghcr.io/gmzone-it/<repo>
#   - SBOM attached to the image (cosign attest)

name: build-and-publish

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

# Only the latest run per branch/PR is kept; older ones are cancelled.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  # github.repository = 'gmzone-it/gmzone-app-template' → image name follows that
  IMAGE_NAME: ${{ github.repository }}

# Default to read-only; each job declares the writes it needs.
permissions:
  contents: read

jobs:

  # ---------------------------------------------------------------------------
  # Job 1: unit tests
  # ---------------------------------------------------------------------------
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r app/requirements.txt -r app/requirements-dev.txt

      - name: Run pytest
        run: pytest app/tests/ -v

  # ---------------------------------------------------------------------------
  # Job 2: security scans (Gitleaks, Semgrep, OWASP Dep-Check)
  # Runs in parallel with 'test' to shorten total pipeline time.
  # ---------------------------------------------------------------------------
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write   # to upload SARIF to the Security tab
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # gitleaks needs full git history, not just last commit

      # ---- 1. Gitleaks (secret scanning, on source + history) -------------
      # We install the binary directly instead of using gitleaks-action,
      # which requires a license on private repos.
      - name: Install Gitleaks
        run: |
          GITLEAKS_VERSION=8.21.2
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            | sudo tar -xzC /usr/local/bin gitleaks

      - name: Run Gitleaks
        id: gitleaks
        continue-on-error: true
        run: gitleaks detect --source . --redact --verbose --no-banner

      # ---- 2. Semgrep (SAST: bugs, antipatterns, security smells) ---------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Semgrep
        # setuptools must be installed explicitly because on Python 3.12+ it is
        # no longer in the standard library, and Semgrep depends on pkg_resources
        # (which lives inside setuptools).
        # We pin setuptools<80 because in setuptools 80+ pkg_resources was REMOVED.
        run: pip install 'setuptools<80' semgrep==1.95.0

      - name: Run Semgrep
        # continue-on-error so dep-check can still run even if semgrep fails;
        # we want ALL scanners to report their findings before the job fails.
        continue-on-error: true
        id: semgrep
        run: |
          semgrep \
            --config=p/security-audit \
            --config=p/python \
            --config=p/dockerfile \
            --severity=ERROR --severity=WARNING \
            --error \
            --sarif --output=semgrep.sarif \
            .

      # ---- Upload Semgrep SARIF to GitHub Code Scanning (Security tab) ----
      # GHAS is free on public repos, so SARIF goes directly to the
      # repository Security → Code scanning alerts page.
      - name: Upload Semgrep SARIF
        if: always() && hashFiles('semgrep.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif
          category: semgrep

      # ---- 3. OWASP Dependency-Check (SCA: CVEs in dependencies) ----------
      # Uses the NVD_API_KEY secret to bypass the unauthenticated rate limit
      # (5 req/30s → 50 req/30s). Set the key once in repo Secrets.
      # if: always() so this scanner runs even if semgrep failed above.
      - name: OWASP Dependency-Check
        if: always()
        continue-on-error: true
        id: depcheck
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: gmzone-app-template
          path: '.'
          format: SARIF
          # --failOnCVSS 7   = fail if any vulnerability with CVSS >= 7.0 (HIGH+)
          # --enableRetired  = scan even retired/EOL libraries
          # --enableExperimental = include experimental analyzers
          args: >
            --nvdApiKey ${{ secrets.NVD_API_KEY }}
            --failOnCVSS 7
            --enableRetired
            --enableExperimental

      - name: Upload Dependency-Check SARIF
        if: always() && hashFiles('reports/dependency-check-report.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: reports/dependency-check-report.sarif
          category: dependency-check

      # ---- Aggregate: human-readable summary in the run page -------------
      # GitHub renders $GITHUB_STEP_SUMMARY as Markdown on the run summary,
      # so we get a quick at-a-glance view without needing to leave the page.
      - name: Write security scan summary
        if: always()
        run: |
          {
            echo "## 🔐 Security scan results"
            echo ""
            echo "| Scanner | Outcome |"
            echo "|---------|---------|"
            echo "| Gitleaks (secrets)         | ${{ steps.gitleaks.outcome || 'n/a' }} |"
            echo "| Semgrep (SAST)             | ${{ steps.semgrep.outcome || 'n/a' }} |"
            echo "| OWASP Dep-Check (CVE)      | ${{ steps.depcheck.outcome || 'n/a' }} |"
            echo ""
            echo "Detailed findings: **Security → Code scanning alerts** in this repo."
          } >> "$GITHUB_STEP_SUMMARY"

      # ---- Fail the job if any scanner reported HIGH/CRITICAL findings ----
      - name: Fail if any security scanner reported issues
        if: |
          steps.gitleaks.outcome == 'failure' ||
          steps.semgrep.outcome == 'failure' ||
          steps.depcheck.outcome == 'failure'
        run: |
          echo "::error::Security scanners reported issues."
          echo "Gitleaks outcome: ${{ steps.gitleaks.outcome }}"
          echo "Semgrep outcome:  ${{ steps.semgrep.outcome }}"
          echo "DepCheck outcome: ${{ steps.depcheck.outcome }}"
          echo "See Security → Code scanning alerts for details."
          exit 1

  # ---------------------------------------------------------------------------
  # Job 3: build, scan image, sign, publish
  # Runs only on push (not on PRs) and only after test+scan have passed.
  # ---------------------------------------------------------------------------
  build-publish:
    name: Build & Publish
    runs-on: ubuntu-latest
    needs: [test, security-scan]
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write          # to push to ghcr.io
      id-token: write          # for cosign keyless OIDC signing
      security-events: write   # for trivy SARIF upload
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Compute tags + labels in one place. Tags rule:
      #   - any push      → 'sha-<short>'
      #   - main branch   → also 'latest'
      #   - tag v1.2.3    → also 'v1.2.3'
      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag

      - name: Build and push image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ github.ref_name }}
            GIT_SHA=${{ github.sha }}
            BUILD_DATE=${{ fromJSON(steps.meta.outputs.json).labels['org.opencontainers.image.created'] }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ---- Trivy: vulnerability scan of the BUILT image -------------------
      # We run Trivy TWICE on purpose:
      #
      # 1) First pass: human-readable table, never fails the job (continue-on-error).
      #    Output goes straight into the Actions log so we can SEE which CVEs are
      #    being flagged without needing to pull the image or download artifacts.
      #
      # 2) Second pass: SARIF format, fails the job on HIGH/CRITICAL findings.
      #    Output uploaded both to GitHub Code Scanning (Security tab) and as
      #    a downloadable artifact for offline inspection.
      #
      # Both passes scan the immutable digest (not a tag) — exactly what we just pushed.

      - name: Trivy image scan (human-readable, non-blocking)
        continue-on-error: true
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          format: table
          severity: HIGH,CRITICAL
          ignore-unfixed: true

      - name: Trivy image scan (SARIF, blocking)
        # Trivy publishes 'master' as the stable, recommended tag (see their
        # official docs). Patch-level tags like 0.28.0 are not always pushed.
        #
        # IMPORTANT: by default, when format=sarif the action ignores the
        # `severity` filter for both the SARIF output AND the exit code,
        # so any single LOW/MEDIUM CVE would fail the build.
        # 'limit-severities-for-sarif: true' makes both the SARIF and the
        # exit code respect `severity: HIGH,CRITICAL`. See:
        # https://github.com/aquasecurity/trivy-action#limit-severities-for-sarif
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          format: sarif
          output: trivy-results.sarif
          severity: HIGH,CRITICAL
          limit-severities-for-sarif: true
          exit-code: '1'
          ignore-unfixed: true

      - name: Upload Trivy SARIF to Code Scanning
        if: always() && hashFiles('trivy-results.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-results.sarif
          category: trivy

      - name: Upload Trivy SARIF as artifact
        # Mirrors the SARIF as a downloadable artifact, so the file can be
        # inspected offline (e.g. `jq '.runs[].results' trivy-results.sarif`).
        if: always() && hashFiles('trivy-results.sarif') != ''
        uses: actions/upload-artifact@v4
        with:
          name: trivy-sarif
          path: trivy-results.sarif
          retention-days: 14

      # ---- SBOM (Software Bill of Materials) via Syft ---------------------
      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}
          format: spdx-json
          output-file: sbom.spdx.json
          upload-artifact: true
          upload-release-assets: false

      # ---- Cosign keyless signing (Sigstore + GitHub OIDC) ----------------
      # No keys to manage — the signature is bound to this workflow's OIDC token.
      # Verifiable by anyone with: cosign verify --certificate-identity-regexp ...
      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign image with Cosign (keyless)
        env:
          COSIGN_EXPERIMENTAL: '1'
        run: |
          cosign sign --yes \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}

      - name: Attest SBOM with Cosign (keyless)
        env:
          COSIGN_EXPERIMENTAL: '1'
        run: |
          cosign attest --yes \
            --predicate sbom.spdx.json \
            --type spdxjson \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.build.outputs.digest }}

      - name: Pipeline summary
        run: |
          echo "### ✅ Image published & signed" >> $GITHUB_STEP_SUMMARY
          echo "**Tags:**" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "**Digest:** \`${{ steps.build.outputs.digest }}\`" >> $GITHUB_STEP_SUMMARY
